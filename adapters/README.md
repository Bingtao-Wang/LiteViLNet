# Adapters

Adapters are thin wrappers that let third-party KITTI Road models run through LiteViLNet evaluation or benchmark tools.

Do not edit upstream code in `third_party/` for normal integration. Add a wrapper here instead.
