name: kitti_road
root: data/kitti_road
input_size: [384, 1248]
modalities: [rgb, adi]
splits:
  train: training
  val: training
  test: testing
notes: "data is a symlink to /home/admin1/Mycode/VLLiNet/datasets/data."
