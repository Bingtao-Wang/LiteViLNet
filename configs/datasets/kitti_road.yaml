name: kitti_road
root: data/kitti_road
input_size: [384, 1248]
modalities: [rgb, adi]
splits:
  train: training
  val: training
  test: testing
notes: "Set root to the local KITTI Road directory; datasets are not distributed with this repository."
